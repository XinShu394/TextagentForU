---
tags: [textagentforu, progress, summary]
updated: 2026-03-23
---

# TextAgentForU — 项目进度总结

> 最后更新：2026-03-23

---

## 一、项目概况

| 维度 | 内容 |
|------|------|
| **项目名称** | TextAgentForU |
| **定位** | 基于企业微信的多用户 AI 生活助手 |
| **核心理念** | 通过对话完成一切——记速记、管待办、写日记、追情绪、养习惯、记账理财 |
| **仓库地址** | `https://github.com/XinShu394/TextagentForU` |
| **官网** | `https://textagent.top` |
| **适用人群** | 2-5 人共享一套部署，每人拥有独立数据空间 |
| **月均成本** | ¥40-110（2-3 人使用） |

---

## 二、品牌名统一（2026-03-23 完成）

### 背景
项目从早期原型历经多次命名变更，代码中存在大量混用。现已全面统一为 **TextAgentForU**。

### 变更范围

| 文件类型 | 涉及文件数 | 替换次数 |
|----------|:---------:|:-------:|
| Python 源码 (`.py`) | 21 | ~85 |
| HTML 页面 (`.html`) | 16 | ~104 |
| Markdown 文档 (`.md`) | 15 | ~290 |
| Shell 脚本 (`.sh`) | 4 | ~55 |
| YAML/Docker 配置 | 3 | ~11 |
| 其他 (`.txt`) | 1 | ~3 |
| **合计** | **60** | **~590** |

### 替换规则

| 原名 | 新名 | 场景 |
|------|------|------|
| 旧名 | 新名 | 场景 |
|------|------|------|
| （旧产品名） | `TextAgentForU` | 产品名/项目名 |
| （旧品牌名） | `TextAgent` | 品牌名（用户面文案、Prompt、代码注释） |
| （旧小写名） | `textagent` | 变量名、路径、cookie key、服务名 |
| （旧大写名） | `TEXTAGENT` | 环境变量/常量 |
| `_TextAgent/` | `_TextAgent/` | 用户数据子目录 |
| `_textagent_system/` | `_textagent_system/` | 系统数据目录 |
| `textagent_token` | `textagent_token` | localStorage/Cookie 键名 |
| `textagent.service` | `textagent.service` | systemd 服务名 |
| `textagent.log` | `textagent.log` | 日志文件名 |

> ✅ **已完成**：文件名/目录名已全部统一重命名（2026-03-24）。

---

## 三、技术架构

### 核心架构图

```
企业微信 → Flask 网关（解密/去重/ASR/异步转发）
    ↓
brain.process()
    ├── 加载 State + Memory（三级缓存，per-user 隔离）
    ├── 三层模型路由 → JSON 决策
    │   ├── Flash (Qwen Flash)        — 陪伴关怀、轻量生成、意图确认
    │   ├── Main  (DeepSeek V3.2)     — 日常路由、Skill 分发
    │   └── Think (DeepSeek V3.2 R1)  — 深度分析、主题深潜
    ├── V4 Flash 智能回复 → 二次生成自然语言
    ├── Agent Loop（最多 5 轮）→ 文件搜索/读取后再回答
    ├── Skill 分发 → 执行操作 → 写回数据
    └── Memory 更新 + 决策日志

V8 智能调度引擎（APScheduler 心跳驱动）
    ├── 待办心跳              （每 1 分钟）
    ├── 智能调度心跳          （每 30 分钟）
    │   ├── 晨报 / 待办提醒 / 主动陪伴
    │   ├── 轻推检测 / 晚间签到 / 日报生成
    │   └── 五层防骚扰 + 用户节奏自学习
    ├── 情绪日记 + 周期调度   （固定 cron）
    ├── 月度回顾 / 财务月报
    └── 每日意图初始化        （05:00）
```

### 技术栈

| 层级 | 技术 |
|------|------|
| 后端框架 | Flask (Python) |
| LLM 主力模型 (Tier 2/3) | DeepSeek V3.2 |
| LLM 轻量模型 (Tier 1) | Qwen Flash (阿里云百炼) |
| LLM 视觉模型 | Qwen VL Max |
| LLM 深度思考 | DeepSeek V3.2 R1 (thinking=on) |
| 语音识别 | 腾讯云 ASR |
| 消息通道 | 企业微信应用 API |
| 定时调度 | APScheduler (V8 智能调度引擎) |
| 数据存储 | 本地文件系统 (JSON/Markdown) + 三级缓存 |
| 前端 | 原生 HTML + Alpine.js + Chart.js |
| 天气服务 | 心知天气 API |
| 部署方式 | Docker / 一键脚本 / 手动 / Git Push |

---

## 四、功能清单

### Skill 列表（39 个 Skill，20 个功能模块）

| 分类 | Skill 命令 | 说明 |
|------|-----------|------|
| **笔记** | `note.save` | 保存到 Quick-Notes |
| | `classify.archive` | 智能归档（工作/情感/趣事/碎碎念） |
| **随记** | `memo.save` / `memo.list` / `memo.search` | 主动随记 + 标签提取 + 查看搜索 |
| **打卡** | `checkin.start` / `checkin.answer` | 每日打卡复盘 |
| **待办** | `todo.add` / `todo.done` / `todo.delete` / `todo.list` / `todo.remind_cancel` | 完整待办管理（增/完成/删/查/取消提醒） |
| **复盘** | `daily.generate` / `weekly.review` / `monthly.review` | 日/周/月报（支持个性化风格） |
| **深潜** | `deep.dive` | 跨时间线主题深度分析 |
| **决策** | `decision.record` / `decision.review` / `decision.list` | 决策记录 + 定期复盘 |
| **数据** | `data.export` / `data.destroy` | 数据导出 + 数据销毁（二次确认） |
| **财务** 🔒 | `finance.import` / `finance.query` / `finance.snapshot` / `finance.monthly` | 账单导入/收支查询/资产快照/月度财报 |
| **语音** | `voice.journal` | 长语音整理为结构化日记 |
| **动态引擎** | `dynamic` | 通用状态操作（带安全白名单） |
| **Agent** | `internal.read` / `internal.search` / `internal.list` | 文件搜索/读取（Agent Loop） |
| **设置** | `settings.nickname` / `settings.ai_name` / `settings.soul` / `settings.info` / `settings.skills` / `settings.report_style` | 个性化设置 + Skill 开关 + 报告风格 |
| **Web** | `web.token` | 生成 Web 查看链接 |

> 🔒 标记的 Skill 仅管理员可用（visibility: private）

### Web 页面（14 个）

| 页面 | 用途 | 鉴权方式 |
|------|------|----------|
| `login.html` | Token 登录 | - |
| `dashboard.html` | 用户概览仪表盘 | Token |
| `notes.html` | 速记查看 | Token |
| `todos.html` | 待办列表 | Token |
| `daily.html` | 日报查看 | Token |
| `archive.html` | 归档笔记 | Token |
| `mood.html` | 情绪日记 | Token |
| `memory.html` | 记忆查看 | Token |
| `decisions.html` | 决策追踪 | Token |
| `habits.html` | 微习惯实验 | Token |
| `reflect.html` | 深度自问 | Token |
| `settings.html` | 个性化设置 | Token |
| `admin.html` | 用户运营中心 | Admin Token |
| `logs.html` | 日志 & 监控中心 | Admin Token |

### Landing 页面（2 个）

| 页面 | 风格 |
|------|------|
| `textagent-foryou.html` | 亮色详细版（6 大功能卡片 + 22 模块网格 + 三步流程 + 架构介绍） |
| `index.html` | 暗色简约版（精简功能展示 + CTA） |

---

## 五、已完成的功能特性 (F-001 ~ F-021)

| ID | 功能 |
|----|------|
| F-001 | 多用户数据隔离：UserContext 全链路贯穿，每用户独立目录树 |
| F-002 | 新用户自动注册 + 三步 Onboarding 引导（昵称→笔记→待办） |
| F-003 | 对话式设置：昵称、AI 人格风格、个人信息、报告风格 |
| F-004 | Web 查看页面（14 页）：完整用户数据展示 + 管理后台 |
| F-005 | Token 令牌鉴权：UUID v4 + 24h 过期 + 三级 fallback 验证 |
| F-006 | 管理员仪表盘：用户列表/LLM 用量统计/成本估算/挂起激活 |
| F-007 | 成本控制：每日消息上限 + LLM 用量追踪 + 不活跃用户跳过 |
| F-008 | Skill 自动发现：39 个 skill handler 零配置注册 |
| F-009 | 三层多模型路由：Flash(Qwen) + Main(DeepSeek V3.2) + Think(DeepSeek R1) |
| F-010 | 先回复后保存：send_fn 回调优化体感延迟 |
| F-011 | 定时任务多用户遍历：随机延迟防限流 + 单用户失败不影响其他 |
| F-012 | V8 智能调度引擎：意图队列 + 心跳驱动 + 规则引擎 + 意图合并 |
| F-013 | 用户节奏自学习：起床/入睡时间推算 + 滑动加权平均 |
| F-014 | V6 动态能力引擎：LLM 自由编排原子操作 + 白名单安全控制 |
| F-015 | Git Push 云服务器部署：一键部署 |
| F-016 | 企微告警推送：慢请求连续检测 + 异常告警 + 冷却防风暴 |
| F-017 | 技术监控中心：三 Tab（日志/统计监控/错误聚合），Sentry 风格 |
| F-018 | 成本预算监控：月成本计算 + 预算进度条 + 超 80% 告警 |
| F-019 | Prompt 膨胀检测：prompt_tokens 分布直方图 + >12K 告警 |
| F-020 | 错误日志聚合：按错误签名去重计数，Top 20 展示 |
| F-021 | 延迟瀑布图增强：可点击展开查看详情 |

---

## 六、已修复的问题

| ID | 问题 | 修复方式 |
|----|------|----------|
| I-001 | 健康检查 GET / 日志刷屏 | 添加 Werkzeug `_HealthCheckFilter` 过滤 |
| I-002 | V8 容器重启后下午推送"早安"晨报 | `_daily_init()` 新增过期意图跳过 |
| I-003 | 节奏学习被下午消息污染 | 入口过滤 wake_time 05:00~12:00，sleep_time 20:00~04:00 |

---

## 七、已知问题（待观察）

| ID | 优先级 | 问题 | 缓解方案 |
|----|:------:|------|----------|
| I-004 | P3 | memory.md 缓存延迟 | TTL 到期自动刷新 / 手动 curl 刷新 |
| I-005 | P3 | 极快连发消息 state 写入竞争 | per-user Lock 已缓解 |

---

## 八、迭代方向

### P1 — 功能增强

| ID | 方向 | 复杂度 |
|----|------|:------:|
| O-001 | SKILLS 按需裁剪（降 prompt_tokens ~30%） | 中 |
| O-002 | V8 Flash LLM 评估推送时机 | 中 |
| O-003 | dynamic custom.* 聚合查询 | 低 |
| O-004 | 节奏数据可视化 | 低 |
| O-005 | 批量导入历史数据 | 中 |
| O-010 | 管理员用户详情页 | 中 |
| O-011 | UptimeRobot 外部监控 | 低 |

### P2 — 架构演进

| ID | 方向 | 复杂度 |
|----|------|:------:|
| O-006 | 本地文件 → SQLite 迁移 | 中 |
| O-007 | Prompt 工程自动化测试框架 | 中 |
| O-008 | 用户规模扩展（10+ 人） | 高 |
| O-009 | GitHub 开源 + CI/CD | 中 |

---

## 九、项目结构

```
TextagentForU/
├── setup.sh                 # 一键安装脚本
├── deploy_textagent.sh      # 一键部署脚本
├── .env.example             # 配置模板
├── src/                     # 核心代码
│   ├── app.py               # Flask 网关 + V8 智能调度引擎
│   ├── brain.py             # AI 大脑（三层模型路由 + Agent Loop）
│   ├── prompts.py           # Prompt 统一管理（V12 结构化 Skill 描述）
│   ├── config.py            # 配置管理
│   ├── user_context.py      # 多用户管理（UserContext）
│   ├── web_routes.py        # Web API + 页面路由
│   ├── memory.py            # 记忆系统（三级缓存）
│   ├── skill_loader.py      # Skill 自动发现 V12
│   ├── channel_router.py    # 渠道路由器
│   ├── storage.py           # 存储抽象层
│   ├── local_io.py          # 本地文件读写
│   ├── wework_crypto.py     # 企微消息加解密
│   ├── wework_rich_message.py # 企微富文本消息
│   ├── finance_utils.py     # 财务工具
│   ├── onedrive_io.py       # OneDrive 读写层
│   ├── requirements.txt     # Python 依赖
│   ├── skills/              # 20 个功能模块 → 39 个 Skill
│   │   ├── checkin_flow.py
│   │   ├── classify_archive.py
│   │   ├── daily_report.py
│   │   ├── data_manage.py
│   │   ├── decision_track.py
│   │   ├── deep_dive.py
│   │   ├── dynamic_engine.py
│   │   ├── finance_import.py
│   │   ├── finance_query.py
│   │   ├── finance_report.py
│   │   ├── finance_snapshot.py
│   │   ├── internal_ops.py
│   │   ├── memo.py
│   │   ├── monthly_review.py
│   │   ├── note_save.py
│   │   ├── settings.py
│   │   ├── todo_manage.py
│   │   ├── voice_journal.py
│   │   ├── web_token.py
│   │   └── weekly_review.py
│   └── web_static/          # 14 个 Web 页面
├── deploy/                  # Docker + SCF 部署配置
├── landing/                 # 官网首页（2 个版本）
├── scripts/                 # 工具脚本
├── tests/                   # 测试
├── docs/                    # 文档
└── data/                    # 数据目录
    ├── _textagent_system/   # 系统数据
    └── users/               # 用户隔离数据
```

---

## 十、变更日志

| 日期 | 变更内容 |
|------|----------|
| 2026-03-24 | 文件名/目录名全量重命名：所有旧品牌相关文件名和目录名统一为 textagent |
| 2026-03-23 | 品牌名全量统一：代码内容中旧品牌名 → TextAgent/TextAgentForU（60 个文件，590 处替换） |
| 2026-03-23 | 仓库地址修正：GitHub URL 统一指向 `XinShu394/TextagentForU` |
| 2026-03-20 | 待办管理功能改进：todo.delete vs todo.done 分离、意图确认、心跳检测 |
| 2026-03-11 | 功能全景图文档完善 |
| 2026-03-10 | 初始部署上线，核心功能就绪 |

---

## 十一、部署信息

### 服务器部署

| 项目 | 值 |
|------|------|
| 服务器路径 | `/opt/textagent/` |
| systemd 服务 | `textagent.service` |
| 日志文件 | `/opt/textagent/textagent.log` |
| 服务端口 | 9000 |

### 快速操作

```bash
# 更新代码
cd /opt/textagent && git pull origin main && sudo systemctl restart textagent

# 查看状态
sudo systemctl status textagent

# 实时日志
journalctl -u textagent -f

# 查看应用日志
tail -f /opt/textagent/textagent.log
```

---

*本文档由 CodeBuddy 自动生成，基于 2026-03-23 的代码库状态。*
